meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/public/login
  body: json
  auth: inherit
}

body:json {
  {
      "username": "{{username}}",
      "password": "{{password}}"
  }
}
